from django.contrib import admin
from django.urls import path, include

urlpatterns = [
    #path('', views.AchievementView.as_view(), name='members-api'),
    #path('<int:id>/', views.SpecificAchievementView.as_view(), name='member-api'),
]
